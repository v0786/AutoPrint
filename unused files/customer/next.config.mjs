/**
 * Next.js configuration for AutoPrint Customer.
 *
 * Standalone mode creates a self-contained production server
 * suitable for Electron and local deployment environments.
 */

/** @type {import("next").NextConfig} */
const nextConfig = {
  output: "standalone",
};

export default nextConfig;